# 시뮬레이터 돌려본 결과 — 문제점과 4가지 개선 항목 매핑

직접 실행해 보면서 확인한 내용을 정리했습니다. 개선 항목 하나 고르실 때 참고하세요.

---

## 담당 정리 (슬랙·메시지 기준)

- **Tien**: 아이콘/UI 요소 **추출(extraction)** 쪽 담당.  
  - 필요한 포맷(투명 배경 등) 확인했고, AMEX 데이터셋이 커서 **디스크 부족**(랩 디스크 ~20GB 남음) 이슈로 **전체가 아니라 subset만 추출**하기로 함.  
  - Namho가 “아이콘 여러 번 저장하고 있나?(e.g. Chrome 아이콘 중복)”라고 물어본 걸 보면, **저장/중복 제거**도 같이 보는 중인 듯.  
  → **3번(파서·추출 품질)** + 저장/디스크 효율과 가깝고, **1번(동일 상태/통합 그래프)** 와는 역할이 다름.
- **sy**: **1번 — 동일 상태 감지 + 통합 그래프** 관심. 작업 브랜치: `sy`.

---

## 1. 실행해서 확인한 것

### ✅ 동작하는 것
- **Synthetic 환경 생성** (`data_engine/tree.py --icon_source synthetic`)
  - `megfile` import만 제거하면 실행 가능 (이미 제거해 둠).
  - 231페이지, 230 아이콘으로 정상 생성. 출력: `ui_environment_448/<timestamp>/` 또는 `--output_dir` 지정 경로.

### ❌ 그대로는 안 되는 것
- **Trajectory 모드** (`--icon_source trajectory`)
  - 필요: OmniParser weights (`/ext_hdd2/nhkoh/OmniParser/weights`), GUIOdyssey `annotations/`, `screenshots/` 경로.
  - 전부 기본값이 **다른 머신 절대 경로**라, 이 PC에서는 경로/데이터 없으면 실패.
- **Sim2Real Compose** (`data_engine/sim2real_compose.py`)
  - 동일하게 OmniParser, GUIOdyssey, `OPENAI_API_KEY` 필요. 경로 하드코딩.
- **평가** (`eval/evaluate.py`)
  - `datas/pages/` 없음. `datas/test_id_edge.json` 등이 가리키는 이미지 경로가 `/ext_hdd2/nhkoh/gelab-env/datas/pages/` 로 되어 있어, 이 환경에서는 그대로 돌리면 실패.

---

## 2. 네 가지 개선 항목 — “지금 코드에서 뭐가 문제인지”

### [ ] 1. 동일 상태 감지 + 통합 그래프

**지금 상태**
- **Synthetic**: 페이지를 이을 때마다 **항상 새 노드** 생성. “같은 화면”이어도 노드를 따로 둠.
- **Trajectory (tree.py)**  
  - `generate_trajectory_family_environment`에서 step마다 `page_0`, `page_1`, … 새로 만들고,  
  - branch 페이지는 “다른 아이콘 클릭”으로 나뉜 뒤 **merge**로 다시 spine으로 합침.  
  - 하지만 **“이 페이지 레이아웃/상태가 이미 있는 노드와 같다”**는 판단은 없음.  
  - 그래서 **동일한 상태가 여러 번 나와도 노드가 계속 늘어남**.

**객관적 문제**
- “같은 앱 화면”이 여러 경로에서 반복되면 노드 수가 불필요하게 많아짐.
- 그래프가 “실제 앱의 상태 그래프”보다 비대해지고, 학습/평가와도 어긋날 수 있음.

**개선 시 할 일**
- 페이지(또는 상태) **동일성 기준** 정의 (예: 레이아웃 해시, 아이콘 집합, 스크린샷 유사도).
- 그래프 빌드 시 **동일 상태면 새 노드 생성 대신 기존 노드에 엣지만 추가**.
- `TopologyBuilder.build` 또는 trajectory 환경 빌드 루프 안에 “상태 비교 → merge” 로직 넣기.

---

### [ ] 2. 액션 스케일·일관성 (GUIOdyssey / AMEX 호환)

**지금 상태**
- **eval/evaluate.py**: 액션은 **click**과 **complete**만 파싱.  
  - `parse_action()`이 `click(start_box='<|box_start|>(x,y)<|box_end|>')` / `complete` 만 처리.
- **좌표**: 0–1000 정규화 한 곳에서 쓰고, bbox 비교도 0–1000 기준.
- **scroll, type, tap** 같은 액션은 **정의/파싱 없음**.  
  - GUIOdyssey·AMEX 데이터를 그대로 쓰려면 포맷 변환 레이어가 필요.

**객관적 문제**
- GELab은 “CLICK + COMPLETE”만 가정해서, 다른 벤치마크(여러 액션 타입)와 **데이터/평가 재사용이 어렵다**.
- 해상도·화면 크기가 바뀌어도 “같은 액션”이 일관되게 스케일되는지 보장하는 공통 레이어가 없음.

**개선 시 할 일**
- 액션 타입 확장: **scroll, type, tap** 등 정의하고, 파서/평가에서 처리.
- 좌표/스케일: **입력(예: 0–1000) ↔ 실제 캔버스(448×448 등)** 변환을 한 모듈로 두고, 페이지/환경이 바뀌어도 동일 규칙 적용.
- GUIOdyssey/AMEX 포맷 → GELab 내부 액션으로 변환하는 스크립트 또는 `prepare_continue_train_data.py` 확장.

---

### [ ] 3. 파서·생성 이미지 품질

**지금 상태**
- **추출(파서)**  
  - `sim2real_compose.py`: YOLO(아이콘) + EasyOCR(텍스트)로 감지·크롭.  
  - `sim2real.py`: 비슷하게 `merge_detections`, `deduplicate_icons` 등으로 후처리.  
  - 가중치/경로가 `/ext_hdd2/...` 등으로 고정돼 있어, 환경 바꾸면 수정 필요.
- **생성 이미지**  
  - `tree.py`의 `RenderEngine`: 448×448 캔버스, 아이콘 `fit_image_to_canvas`로 배치.  
  - Sim2Real은 GPT로 레이아웃 생성 후 `render_from_code` 등으로 그리는데, **품질 이슈**(겹침, 비율, 해상도)는 코드/이슈에 따라 다름.

**객관적 문제**
- 파서: bbox/레이블 품질이 들쭉날쭉하면, 그대로 쓴 환경이 “현실적이지 않다”고 느껴짐.
- 생성: 아이콘 크기·비율·정렬이 부자연스러우면 시뮬레이터가 “가짜처럼” 보이고, 학습/평가에도 영향.

**개선 시 할 일**
- 파서: 감지 임계값, NMS, OCR 후처리, bbox 정규화 등으로 **정확도·일관성** 개선.
- 렌더링: `RenderEngine` / Sim2Real 렌더에서 **크기·비율·여백·해상도** 정책을 정하고, 필요하면 품질 메트릭이나 시각 검수로 개선 포인트 찾기.

---

### [ ] 4. 시각/레이아웃 변형 생성 (GPT 등)

**지금 상태**
- **색·형태·레이아웃을 “조절 가능한 파라미터”로 두고 생성하는 코드는 없음.**  
  - Synthetic은 고정 아이콘 풀; trajectory는 “실제 크롭”을 그대로 배치.
- **sim2real_compose.py**에서는 GPT로 **페이지 레이아웃/구성**을 생성하지만,  
  - “같은 구조에서 색만 바꿔줘”, “아이콘 모양만 바꿔줘” 같은 **제어 가능한 변형**은 없음.

**객관적 문제**
- 데이터 다양화를 위해 “같은 논리 구조, 다른 스킨”을 만들고 싶어도, **지금은 수동/스크립트 수준**으로만 가능.
- 태승님 메시지의 “controllable visual and layout variations”에 해당하는 **공통 파이프라인**이 없음.

**개선 시 할 일**
- **시각 변형**: 아이콘/배경의 **색·채도·밝기**를 인자로 받는 렌더 옵션 또는 전처리 단계 추가. (예: 기존에 쓰던 perturbation을 “생성 단계”에 통합.)
- **레이아웃 변형**: 그리드 오프셋, 간격, 아이콘 크기 등 **파라미터화**해서 같은 구조에서 여러 레이아웃 생성.
- **(선택)** GPT 호출: “이 아이콘을 파란색으로”, “버튼을 둥글게” 같은 지시 → 색/도형 파라미터로 반영하는 레이어.

---

## 3. 한 줄 요약 — 뭘 고르면 좋을지

| 항목 | 지금 문제 | 담당하기 좋은 이유 |
|------|-----------|---------------------|
| **1. 동일 상태 + 통합 그래프** | 같은 화면도 매번 새 노드 → 그래프 비대 | 구조/그래프 설계에 관심 있으면, 트리·상태 정의부터 손대기 좋음. |
| **2. 액션 스케일·일관성** | click/complete만 있음, scroll/type/tap 없음, 외부 데이터와 포맷 불일치 | GUIOdyssey/AMEX 연동이나 평가 파이프라인 확장하고 싶을 때 적합. |
| **3. 파서·이미지 품질** | 감지/렌더 품질이 환경·데이터에 따라 들쭉날쭉 | 비전·파서·렌더링 품질 개선에 재미붙이기 좋음. |
| **4. 시각/레이아웃 변형** | “조절 가능한” 변형 파이프라인이 없음 | 데이터 증강, GPT 활용, 시각 다양화에 관심 있으면 맞음. |

---

## 4. 참고 — 이번에 한 수정

- **data_engine/tree.py**: 사용처 없는 `from megfile import ...` 제거 → `--icon_source synthetic` 실행 가능하도록 함.

원하시는 항목 정해지면, 그 항목만 골라서 “구체적으로 어느 파일/함수부터 손대면 좋을지” 단계별로 더 쪼개 드릴 수 있습니다.
